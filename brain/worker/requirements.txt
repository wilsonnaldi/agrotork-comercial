# Worker de ingestão do AGROTORK BRAIN (Fase 2, Lote B). Sem vetor.
pdfplumber>=0.11,<0.12
openpyxl>=3.1,<3.2
psycopg[binary]>=3.2,<4
# OCR é opcional: só entra se o binário `tesseract` existir na máquina.
pytesseract>=0.3.10
pytest>=8
reportlab>=4        # só para os testes (gera PDFs sintéticos)
